/// Type of [CarouselIndicator].
enum PageIndicatorType {
  /// Indicator that shows current page with color.
  colored,

  /// Indicator that shows current page throug horizontal expantion.
  expanded,
}
